#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>
#include <time.h>

int main(){

	int pid[3];
	for(int i = 0; i < 3; i++){
		
		if((pid[i] = fork()) == 0){
			sleep(60);
			return 0;
		}
	}

	for(int i = 0; i<3;i++){
		wait(NULL);
	}
	return 0;
}